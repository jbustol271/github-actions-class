name: Despliegue Continuo (docker actions)

on:
  workflow_dispatch:
    
jobs:
  buildAndPushImage:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Login into Docker Hub
        uses: docker/login-action@v4
        with:
          username: erkapitan
          password: ${{ secrets.DOCKER_PASSWORD}}
          