name: Frontend Despliegue Continuo (docker actions) - cd_frontend_with_actions.yaml

on:
  workflow_dispatch:
    
jobs:
  buildAndPushImage:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Login into Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ vars.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD}}
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
      - name: Build and push Docker Image
        uses: docker/build-push-action@v7
        with:
          context: ./hangman-front
          push: true
          tags: erkapitan/hangman-front-cep-grupo-a-cd_frontend_with_actions:latest
          file: ./hangman-front/Dockerfile
